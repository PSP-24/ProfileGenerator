# ProfileGenerator

Installing auth is a prerequisite for this package. The functionality will not work otherwise.

composer require laravel/ui

php artisan ui vue --auth

composer require psp-24/profilegenerator
